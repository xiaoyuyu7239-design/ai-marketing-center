# 版本索引

更新时间：2026-07-05 22:56:55 CST

## 版本存档根目录

`/Users/xiaoyu/Desktop/ai营销中台/版本存档`

## 命名规则

- 版本文件夹统一命名为：`版本001`、`版本002`、`版本003`。
- 后续版本继续放在 `版本存档` 根目录下。
- 归档时复制项目根目录内容，但必须排除 `版本存档` 目录本身。

## 版本001

路径：

`/Users/xiaoyu/Desktop/ai营销中台/版本存档/版本001`

归档状态：

- 已创建版本文件夹。
- 已从当前项目完整复制归档。
- 复制时已排除外层 `版本存档` 目录，避免递归复制。
- 归档大小约 `9.8G`。
- 归档检查时源项目与 `版本001` 文件数量一致：`151883` 个文件，`166974` 个总条目。
- 已包含源码、配置、`.git`、`.next`、`node_modules`、媒体资源等当前版本内容。

验证状态：

- 已启动验证。
- 启动地址：`http://127.0.0.1:3001`
- `/start` 页面可正常访问和渲染。
- `/project/agent` 工作台页面可正常访问。
- 核心静态资源可正常返回。

备注：

- `版本001` 是用户当前满意版本的存档。
- 除非用户明确要求，不要直接修改已有版本存档内容。
