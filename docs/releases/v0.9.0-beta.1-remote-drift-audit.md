# v0.9.0-beta.1 远端漂移与发布边界审计

审计时间：2026-07-16 23:25 CST  
状态更新：2026-07-17 11:29 CST

## 结论

`171f579d56d6531d31ee7ec722232c586e232f38` 是已完成本地全量验证并首次推入私有发布线的邀请内测候选快照；它仍不适合直接合入原公开仓库 `xixihhhh/clipforge` 的 `main`。产品负责人已选择独立私有邀请内测发布线 `xiaoyuyu7239-design/huimai-invite-beta`，当前私有 `main` 已推进到 `fa266c5ab6d986e2f67d8180583094892dd3e78a`。首次 CI 的媒体二进制路径问题已经修复；该最终提交对应的 CI `29523621458` 与 Docker 发布运行 `29523668544` 均成功，Linux/amd64 镜像已完成容器冒烟、GHCR 推送、远端摘要反查和结构化证据校验。首位外部邀请仍是 **No-Go**：已完成的是可部署制品，不是真实公网 HTTPS 网站；Gate 1–5 中的生产域名、服务器、真实配置、模型评测、边缘探针、备份运营和生产预检仍未闭环。

## 已核验事实

- GitHub 仓库 `xixihhhh/clipforge` 当前为公开仓库。
- 远端 `main` 在本次获准 fetch 时为 `dd6d785fb153e49bb9e99dc5d7ee71daf8ad4b15`，相对候选基线 `dbb0868af1d69c4d53a72efe974d08364cb5cd88` 前进 78 个提交。
- fetch 到的标签中没有 `v0.9.0-beta.1`；这只代表本次 fetch 时不存在同名远端标签，创建前仍须再次 fetch。
- 候选单提交改动 566 个文件；远端 78 个提交改动 258 个文件，两边有 110 个路径重叠。
- 在全新隔离副本执行三方合并预演后，出现大量内容、改删和目录迁移冲突；即使冲突优先保留候选侧，TypeScript 仍暴露路由、数据库、提示词、视频合成和新旧目录双轨等广泛不一致。该预演不能作为 release candidate。
- `gh 2.96.0` 已安装；后续已恢复账号 `xiaoyuyu7239-design` 的登录，并创建私有仓库 `xiaoyuyu7239-design/huimai-invite-beta`。仓库已核验为 Private，默认分支为 `main`；初始候选 `171f579d56d6531d31ee7ec722232c586e232f38` 已推送并核验，当前远端 `main` 精确等于 `fa266c5ab6d986e2f67d8180583094892dd3e78a`。未创建 tag 或 GitHub Release，原公开仓库未被改动。
- 首次 CI 运行 `29515180359` 在 ESLint 与 TypeScript 通过后，于单元测试阶段失败：CI 已成功安装 `ffmpeg-static 5.3.0` 与 Linux ffprobe，但测试环境未设置 `FFMPEG_PATH` / `FFPROBE_PATH`，路径解析退回 Ubuntu PATH 中不存在的系统命令，导致 3 个测试文件、6 项用例失败；该记录保留为历史事故证据。
- 私有发布线通过 5 个 PR 依次修复 CI 媒体路径、手动触发能力、Docker 发布门禁媒体路径、生产镜像 FFmpeg `drawtext` 能力，以及用户所有私有仓库不支持 GitHub Artifact Attestations 的平台限制。最终提交的独立 CI `29523621458` 与 Docker 发布运行 `29523668544` 均为 `success`。
- 最终 Linux/amd64 镜像为 `ghcr.io/xiaoyuyu7239-design/huimai-invite-beta@sha256:3e6446a69eba99bc7a3e15f47c93496e1d1307e87397da4fe9f502d89a7ad8d8`；已测试 image/config digest 为 `sha256:f7573633f9bf870737aa21c165f2431262ff3e08f5adfeeb9d5838e9be8e10a7`。远端 registry manifest 文件 SHA-256 与不可变镜像摘要精确一致。
- 发布证据 artifact 为 `docker-release-evidence-fa266c5ab6d986e2f67d8180583094892dd3e78a`，记录 551 个 Git-tracked 发布文件、98,409,635 字节和源码摘要 `eefb2946fc85a947e97fe110a5589bdc55a6fef4c425b135331b4a9e743e5960`。证据明确记录 `repositoryPrivate: true` 与 `artifactAttestationPolicy: skipped-private-repository`；未通过把仓库改为公开来绕过平台限制。
- 原公开仓库漂移审计与三方合并预演全程未 push、未创建远端分支、未打 tag、未创建 PR/Release，也未修改原仓库 Git 索引。

## 额外公开发布风险

- 候选新增 16 个 MP4，共 86,754,192 bytes，最大单文件 17,273,803 bytes；它们均被页面引用。
- 产品负责人已确认这 16 个 MP4 均为自有或已获授权，可用于本次私有邀请内测发布。该确认解除本次私有发布的素材阻塞；未来若改为公开分发，仍建议补充逐文件来源、授权范围与 hash 台账，并按需要迁移到受控对象存储/CDN。

## 推荐发布策略

邀请内测继续执行功能冻结，不把原公开仓库 78 个未经本轮安全、租户、计费和模型门禁验证的新功能临时塞入 `v0.9.0-beta.1`。发布策略已经确定为独立私有邀请内测发布线，不整合原公开版 `main` 的新增提交。远程 CI、Linux Docker smoke、GHCR 不可变摘要和结构化发布证据已经闭环；下一步是用该不可变摘要准备真实 Linux x86_64 单实例服务器、生产域名/DNS/TLS、Nginx、生产配置与持久卷，再依序完成模型评测、边缘探针、备份运营、生产 preflight 和外网浏览器冒烟。在真实 HTTPS 网址及 Gate 1–5 完成前，不得邀请外部商户。
